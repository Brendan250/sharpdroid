<!--
  the chevron on a card that opens, pointing down while it is closed.

  it is rotated 180 degrees rather than swapped for a second drawable, so the turn is what says the
  card is opening. see AboutActivity.

  Material's `expand_more`, at Material Symbols' 960 viewport, tinted by the layout.
-->
<vector xmlns:android="http://schemas.android.com/apk/res/android"
    android:width="24dp"
    android:height="24dp"
    android:viewportWidth="960"
    android:viewportHeight="960">
    <path
        android:fillColor="@android:color/white"
        android:pathData="M480,600L280,400L336,344L480,488L624,344L680,400L480,600Z" />
</vector>
